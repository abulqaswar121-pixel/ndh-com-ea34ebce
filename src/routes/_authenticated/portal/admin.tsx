import { createFileRoute } from '@tanstack/react-router'; import { useEffect,useState } from 'react'; import { ClipboardCheck, FileText, KeyRound, Settings2, UsersRound } from 'lucide-react'; import { supabase } from '@/integrations/supabase/client'; import { PageShell } from '@/components/PageShell'; import { RequireRole } from '@/components/RequireRole'; import { Reveal } from '@/components/Reveal'; import { reviewSubmission,createInvite,setPMReviewAccess } from '@/lib/admin.functions';
export const Route=createFileRoute('/_authenticated/portal/admin')({component:()=> <RequireRole role="admin"><AdminPortal/></RequireRole>}); function AdminPortal(){const [reviews,setReviews]=useState<any[]>([]);const [email,setEmail]=useState('');const [name,setName]=useState('');const [link,setLink]=useState('');const [pmId,setPmId]=useState('');async function load(){const {data}=await (supabase as any).from('academy_submissions').select('*').eq('status','submitted').order('created_at',{ascending:false});setReviews(data??[])}useEffect(()=>{void load()},[]);async function invite(role:'talent'|'pm'){const r=await createInvite({data:{role,email,fullName:name}});setLink(`${location.origin}${r.url}`)}return <PageShell><main className="portal"><div className="portal-head"><div><p className="eyebrow">ADMIN PORTAL</p><h1>Operate the hub.</h1><p>Review work, manage access, and keep the Academy moving.</p></div><Settings2 size={42}/></div><Reveal><section className="portal-section"><div className="portal-section-title"><h2>Project Review Queue</h2><ClipboardCheck size={22}/></div>{reviews.length===0?<div className="empty-card">No open project reviews.</div>:<div className="portal-grid">{reviews.map(r=><article className="portal-card" key={r.id}><ClipboardCheck size={20}/><h3>Student submission</h3><p>{r.ai_verdict||'AI verdict pending'}</p><a href={r.submission_url||'#'}>View submission</a><div className="project-actions"><button className="button" onClick={async()=>{await reviewSubmission({data:{submissionId:r.id,decision:'approved'}});await load()}}>Approve &amp; Sign</button><button onClick={async()=>{await reviewSubmission({data:{submissionId:r.id,decision:'rejected',note:'Please revise and resubmit.'}});await load()}}>Reject</button></div></article>)}</div>}</section></Reveal><Reveal><section className="portal-section"><div className="portal-section-title"><h2>Invitations</h2><KeyRound size={22}/></div><div className="auth-form inline-form"><input placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="button" onClick={()=>invite('talent')}>Invite as Talent</button><button className="button button-secondary" onClick={()=>invite('pm')}>Invite PM</button></div>{link&&<div className="empty-card">Invite link: {link}</div>}</section></Reveal><Reveal><section className="portal-section"><div className="portal-section-title"><h2>PM review access</h2><UsersRound size={22}/></div><div className="auth-form inline-form"><input placeholder="PM user ID" value={pmId} onChange={e=>setPmId(e.target.value)}/><button className="button" onClick={()=>setPMReviewAccess({data:{pmId,enabled:true}})}>Enable review queue</button><button className="button button-secondary" onClick={()=>setPMReviewAccess({data:{pmId,enabled:false}})}>Disable</button></div></section></Reveal><Reveal><section className="portal-section"><div className="portal-section-title"><h2>Payout requests</h2><ClipboardCheck size={22}/></div><PayoutQueue/></section></Reveal><Reveal><section className="portal-section"><div className="portal-section-title"><h2>User management</h2><UsersRound size={22}/></div><UserSearch/></section></Reveal><Reveal><section className="portal-section"><div className="portal-section-title"><h2>Course Editor</h2><FileText size={22}/></div><CourseEditor/></section></Reveal></main></PageShell>} function PayoutQueue(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(supabase as any).from('payout_requests').select('*').eq('status','pending').then(({data}:any)=>setRows(data??[]))},[]);async function decide(id:string,status:'approved'|'rejected'){await (supabase as any).from('payout_requests').update({status,processed_at:new Date().toISOString()}).eq('id',id);setRows(x=>x.filter(r=>r.id!==id))}return rows.length===0?<div className="empty-card">No pending payout requests.</div>:<div className="portal-grid">{rows.map(r=><article className="portal-card" key={r.id}><ClipboardCheck size={20}/><h3>{r.currency} {Number(r.amount).toLocaleString()}</h3><p>Talent: {r.talent_id}</p><div className="project-actions"><button className="button" onClick={()=>decide(r.id,'approved')}>Approve</button><button onClick={()=>decide(r.id,'rejected')}>Reject</button></div></article>)}</div>} function UserSearch(){const [q,setQ]=useState('');const [users,setUsers]=useState<any[]>([]);async function search(){const {data}=await (supabase as any).from('profiles').select('id,full_name,email').or(`full_name.ilike.%${q}%,email.ilike.%${q}%`).limit(20);setUsers(data??[])}return <><div className="auth-form inline-form"><input placeholder="Search name or email" value={q} onChange={e=>setQ(e.target.value)}/><button className="button" onClick={search}>Search</button></div>{users.length>0&&<div className="portal-grid">{users.map(u=><article className="portal-card" key={u.id}><UsersRound size={20}/><h3>{u.full_name||'Unnamed user'}</h3><p>{u.email}</p><p className="admin-note">Role changes are restricted to Admin actions.</p></article>)}</div>}</>} function CourseEditor(){const [courses,setCourses]=useState<any[]>([]);const [selected,setSelected]=useState<any>();const [saved,setSaved]=useState(false);useEffect(()=>{(supabase as any).from('courses').select('*').order('title').then(({data}:any)=>setCourses(data??[]))},[]);function pick(id:string){setSelected(courses.find(c=>c.id===id))}async function save(e:React.FormEvent){e.preventDefault();if(!selected)return;const {data}=await (supabase as any).from('courses').update({title:selected.title,summary:selected.summary,learning_objectives:selected.learning_objectives,project_theme:selected.project_theme,is_published:selected.is_published}).eq('id',selected.id).select('*').single();if(data){setCourses(x=>x.map(c=>c.id===data.id?data:c));setSelected(data);setSaved(true)}}return <div className="editor"><select value={selected?.id||''} onChange={e=>pick(e.target.value)}><option value="">Select a course</option>{courses.map(c=><option key={c.id} value={c.id}>{c.title}</option>)}</select>{selected&&<form className="auth-form" onSubmit={save}><label>Title<input value={selected.title} onChange={e=>setSelected({...selected,title:e.target.value})}/></label><label>Summary<textarea value={selected.summary||''} onChange={e=>setSelected({...selected,summary:e.target.value})}/></label><label>Learning Objectives<textarea value={selected.learning_objectives||''} onChange={e=>setSelected({...selected,learning_objectives:e.target.value})}/></label><label>Project Theme<textarea value={selected.project_theme||''} onChange={e=>setSelected({...selected,project_theme:e.target.value})}/></label><button className="button">Save course</button>{saved&&<p>Course changes saved.</p>}</form>}</div>}
