import { createServerFn } from '@tanstack/react-start'; import { z } from 'zod'; import { requireSupabaseAuth } from '@/integrations/supabase/auth-middleware';
export const initializeCoursePayment=createServerFn({method:'POST'}).middleware([requireSupabaseAuth]).inputValidator((d:unknown)=>z.object({courseId:z.string().uuid(),email:z.string().email(),amount:z.number().positive(),currency:z.string().default('NGN')}).parse(d)).handler(async({data,context})=>{const key=process.env.PAYSTACK_SECRET_KEY;if(!key)throw new Error('Payment provider is not configured');const response=await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({email:data.email,amount:Math.round(data.amount*100),currency:data.currency,metadata:{student_id:context.userId,course_id:data.courseId}})});if(!response.ok)throw new Error('Unable to initialize payment');const body=await response.json() as any;return body.data as {authorization_url:string;reference:string;access_code:string};});
